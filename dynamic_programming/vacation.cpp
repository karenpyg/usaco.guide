#include <bits/stdc++.h>
using namespace std; 

const int MAX_INT = 1e5; 
int dp[MAX_INT][3]; 

int main()
{
    memset(dp, -1, sizeof(dp));
    
    int N ; cin >> N; 
    for (int i = 0; i < N; i++){
        int a, b, c ; cin >> a >> b >> c ; 


    }
}